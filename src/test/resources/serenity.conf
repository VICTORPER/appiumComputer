environments {
  default {
    webdriver.base.url = "http://demo.testim.io/"
  }
   default {
        webdriver.map.url = "http://demo.testim.io/"
      }
}

#crentials encoding - Recommendation: Use time-bound credentials

credentials {
  default {
    user.name = "Test"
  }
  default {
    pass = "c2VjcmV0X3NhdWNl"
  }
}

serenity {
  encoding = "UTF-8"
  compress.filenames = true
  take.screenshots = FOR_EACH_ACTION
}